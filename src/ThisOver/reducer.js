const initialState = {
  currentOver: [
    '1',
    'w',
    '6-N',
    'd',
    '2-b',
    '1-lb',
  ],
  currentBowlerId: 1,
};

const thisOverReducer = function thisOverReducer(state = initialState) {
  return state;
};

export default thisOverReducer;
